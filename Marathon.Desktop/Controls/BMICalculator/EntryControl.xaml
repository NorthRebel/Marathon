<UserControl x:Class="Marathon.Desktop.Controls.BMICalculator.EntryControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Marathon.Desktop.Controls.BMICalculator"
             xmlns:input="clr-namespace:Marathon.Desktop.Controls.Input"
             xmlns:viewModels="clr-namespace:Marathon.Core.ViewModel.Input;assembly=Marathon.Core"
             mc:Ignorable="d" 
             d:DesignHeight="50" d:DesignWidth="250">
    <Grid>

        <Grid.ColumnDefinitions>
            <!--Input-->
            <ColumnDefinition Width="*"/>
            <!--Unit-->
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>

        <!--Input-->
        <input:LeftEntryControl
            Grid.Column="0"
            DataContext="{Binding RelativeSource={RelativeSource FindAncestor, 
                AncestorType={x:Type UserControl}}, Path=DataContext}">

            <TextBox Text="{Binding Value}"/>

        </input:LeftEntryControl>

        <TextBlock Grid.Column="1" 
                   x:Name="UnitText"
                   VerticalAlignment="Center"/>
    </Grid>
</UserControl>
